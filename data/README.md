# Fare data

The CSV files in this folder are populated by `.github/workflows/sync-mtr-data.yml` from MTR Corporation's official Open Data endpoints. They are intentionally not fetched by the app during each fare lookup.
